name: cookbook-dev
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - mystmd
  - jupyterlab-myst

name: gridradpy
channels: 
    - pip
dependencies:
    - xarray
    - cartopy
    - matplotlib
    - numpy

name: era5
channels:
    - conda-forge
dependencies:
    - xarray
    - numpy
    - metpy
    - cartopy
    - matplotlib